{% load_json as rawmap %}
{
    "accessid": "access_id",
    "accesskey": "access_key",
    "sources": "/sumo/sources.json"
    "ephemeral": "false"
    "name": {{ salt['grains.get']('nodename') }}
}
{% endload %}
